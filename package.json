{
  "name": "obraflow",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "dev:frontend": "npm run dev --workspace frontend",
    "build": "npm run build --workspace frontend",
    "lint": "npm run lint --workspace frontend",
    "type-check": "npm run type-check --workspace frontend",
    "backend:up": "docker compose -f backend/docker-compose.yml up -d backend",
    "backend:logs": "docker compose -f backend/docker-compose.yml logs -f backend",
    "backend:migrate": "docker compose -f backend/docker-compose.yml exec backend python manage.py migrate",
    "backend:shell": "docker compose -f backend/docker-compose.yml exec backend python manage.py shell",
    "backend:superuser": "docker compose -f backend/docker-compose.yml exec backend bash scripts/create_superuser.sh",
    "backend:test": "docker compose -f backend/docker-compose.test.yml run --rm test"
  }
}
